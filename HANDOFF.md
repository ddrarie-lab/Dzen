# HANDOFF — где мы сейчас

> Живой статус проекта Dzen. Его читает Claude в начале КАЖДОЙ сессии, откуда бы ты ни
> зашла: веб/приложение, CLI на ПК, Termius на телефоне, TG-бот на сервере.
> Файл едет через GitHub автоматически (`tools/continuity/sync.sh`), поэтому все
> точки входа видят одно и то же. Новое пиши СВЕРХУ.
>
> Правило: сюда — состояние и следующие шаги, НЕ секреты. Токены и пароли живут
> в `.env` и в `.memory/secrets.local.md` (оба вне git).

## Следующие шаги

- (пусто — заполнится в конце первой сессии)

## Журнал

### 13.09.2026 — заведён HANDOFF

Подключена непрерывность сессий между точками входа.
